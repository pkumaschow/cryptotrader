name: Publish Docker

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      security-events: write

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      # PRs: load locally (no attestations — required for Scout local analysis)
      - name: Build and load (PR)
        if: github.event_name == 'pull_request'
        uses: docker/build-push-action@v6
        with:
          context: .
          load: true
          tags: pkumaschow/cryptotrader:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # main: push with full supply chain attestations
      - name: Build and push (main)
        if: github.event_name == 'push'
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: pkumaschow/cryptotrader:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
          provenance: mode=max
          sbom: true

      # PRs: compare against production, comment findings on the PR
      - name: Docker Scout compare
        if: github.event_name == 'pull_request'
        uses: docker/scout-action@v1
        with:
          command: compare
          image: pkumaschow/cryptotrader:latest
          to-env: production
          ignore-unchanged: true
          only-severities: critical,high
          github-token: ${{ secrets.GITHUB_TOKEN }}

      # main: record CVEs against the production environment after push
      - name: Docker Scout record
        if: github.event_name == 'push'
        uses: docker/scout-action@v1
        with:
          command: cves
          image: pkumaschow/cryptotrader:latest
          only-severities: critical,high
          github-token: ${{ secrets.GITHUB_TOKEN }}
