name: Publish Docker

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      security-events: write

    steps:
      - uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      # PRs: load amd64 locally (load: true only supports a single platform)
      - name: Build and load (PR)
        if: github.event_name == 'pull_request'
        uses: docker/build-push-action@v6
        with:
          context: .
          load: true
          pull: true
          platforms: linux/amd64
          build-args: |
            APT_REFRESH=${{ github.run_id }}
          tags: pkumaschow/cryptotrader:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # main: push multi-arch manifest with full supply chain attestations
      - name: Build and push (main)
        if: github.event_name == 'push'
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          pull: true
          platforms: linux/amd64,linux/arm64
          build-args: |
            APT_REFRESH=${{ github.run_id }}
          tags: pkumaschow/cryptotrader:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
          provenance: mode=max
          sbom: true

      # PRs: compare against production, comment findings on the PR
      - name: Docker Scout compare
        if: github.event_name == 'pull_request'
        continue-on-error: true
        uses: docker/scout-action@v1
        with:
          command: compare
          image: pkumaschow/cryptotrader:latest
          to-env: production
          ignore-unchanged: true
          only-severities: critical,high
          github-token: ${{ secrets.GITHUB_TOKEN }}

      # main: record CVEs against the production environment after push
      - name: Docker Scout record
        if: github.event_name == 'push'
        continue-on-error: true
        uses: docker/scout-action@v1
        with:
          command: cves
          image: pkumaschow/cryptotrader:latest
          only-severities: critical,high
          github-token: ${{ secrets.GITHUB_TOKEN }}
